meta {
  name: Admin Deactivate Space
  type: http
  seq: 9
}

patch {
  url: {{baseUrl}}/api/admin/spaces/{{spaceId}}/deactivate
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
}
