meta {
  name: Admin Booking Operations
  type: http
  seq: 15
}

get {
  url: {{baseUrl}}/api/admin/bookings?status=upcoming&range=month&limit=100&search={{adminBookingSearch}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{adminToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
