meta {
  name: Admin List Contact Requests
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/admin/contact-requests?readState=unread&sort=received_desc
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{adminToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
